{
  "permissions": {
    "allow": [
      "# ============================================================================",
      "# CLAUDE LOCAL SETTINGS TEMPLATE",
      "# ============================================================================",
      "# This file should NEVER be committed to version control.",
      "# Add .claude/settings.local.json to .gitignore (already done).",
      "",
      "# IMPORTANT SECURITY NOTES:",
      "# 1. Never hardcode API tokens, bearer tokens, or passwords",
      "# 2. Never include absolute local file paths",
      "# 3. Keep sensitive data in environment variables (.env) instead",
      "# 4. Use placeholder values below and replace with actual values locally only",
      "",
      "# ============================================================================",
      "# SAFE EXAMPLE PERMISSIONS (replace with your actual needs)",
      "# ============================================================================",
      "",
      "# File reading (use relative paths only)",
      "\"Read(.claude/plans/**)\",",
      "\"Read(.claude/**)\",",
      "",
      "# Node.js operations",
      "\"Bash(node --check client/main.js)\",",
      "\"Bash(node --check client/engine/spin-engine.js)\",",
      "\"Bash(npm --version)\",",
      "\"Bash(npm test)\",",
      "",
      "# Development server (use environment variables for ports)",
      "\"Bash(npm run dev)\",",
      "\"Bash(npm run build)\",",
      "",
      "# Git operations",
      "\"Bash(git status)\",",
      "\"Bash(git add .)\",",
      "\"Bash(git commit -m)\",",
      "\"Bash(git push)\",",
      "",
      "# Environment-based API calls (use $ADMIN_TOKEN variable)",
      "\"Bash(curl -s http://127.0.0.1:8090/health)\",",
      "",
      "# ============================================================================",
      "# CREDENTIALS - USE ENVIRONMENT VARIABLES ONLY",
      "# ============================================================================",
      "",
      "# Create a .env file with these variables instead:",
      "# ADMIN_TOKEN=your_bearer_token_here",
      "# API_PORT=8090",
      "# PLAYER_PORT=8091",
      "",
      "# Then use in bash commands like:",
      "# Bash(curl -H \"Authorization: Bearer $ADMIN_TOKEN\" http://127.0.0.1:$API_PORT/admin/v1/rounds)",
      ""
    ]
  }
}
